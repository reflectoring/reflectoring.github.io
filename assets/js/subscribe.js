$( document ).ready(function() {
    // show the subscribe popup when the URL ends with "#subscribe"
    let url = window.location.href;
    if(url.endsWith("#subscribe")){
      ml_webform_1449648('show')
    }
});